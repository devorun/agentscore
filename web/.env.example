# AgentScoreRegistry address on Arc Testnet. Leave empty until deployed (Phase 6).
VITE_REGISTRY_ADDRESS=
# Block the registry was deployed at (narrows event scans). Leave empty until deployed.
VITE_REGISTRY_DEPLOY_BLOCK=
