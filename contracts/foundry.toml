[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 200

[fuzz]
runs = 512

# Arc Testnet (verified in Phase 0)
[rpc_endpoints]
arc_testnet = "https://rpc.testnet.arc.network"
